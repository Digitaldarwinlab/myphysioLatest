//This index.js file is temporary. Instead of this, we will include all the apis like UserAuth file
// which will include REST api method like user registeration, user login, user profile etc.
const Index = ()=>{
    return <h1>Hello From API</h1>
}
export default Index;